# PRD 疑问清单

> 基于 `企业文档管理系统-产品需求说明文档.md`（v2.1）逐节分析，整理出需要产品侧澄清的问题。
> 分析日期：2026-04-14
> 更新日期：2026-04-15

### 状态说明

| 标记 | 含义 |
|------|------|
| ✅ 已确认 | 已有明确结论，可进入开发 |
| ⏳ 确认中 | 正在与产品沟通，暂不影响开发 |
| ⬜ 待确认 | 尚未沟通，需产品澄清 |

---

## 一、文档结构与格式问题

| # | 位置 | 问题 | 说明 |
|---|------|------|------|
| D1 | §2.1 文档目的 | ✅ 已确认：无伤大雅 | `### 2.1文档目的` 下方直接跳到了 `### 2.2飞书集成五个触点`，文档目的章节内容缺失 |
| D2 | §6.4.2 | ✅ 已确认：无伤大雅 | "我发起的"和"我已处理"均编号为 `6.4.2`，后者应为 `6.4.3` |
| D3 | §6.3.2 按部门（2） | ✅ 已确认：无伤大雅 | "部门负责人：**可以创建组吗**" → 应为"可以创建组，" |
| D4 | §6.3.2 按产品线（6） | ⏳ 确认中 | 产品线管理员 TAB2 中 toast 文案写的是"已指派 {姓名} 为**部门**管理员"，应为"产品线管理员" |
| D5 | §6.5.2 操作矩阵 | ✅ 已确认：无伤大雅 | 【编辑】按钮行为引用"详见 §6.4.5"，但 6.4 是审批中心，编辑副本确认弹窗应在 §6.3.3 |
| D6 | §6.1 流程图 | ✅ 已确认：无伤大雅 | PRD 中写的是 `flow-v20.html`，实际文件名为 `flow-v20.0.html`（多一个 `.0`） |
| D7 | §6.2 原型图 | ✅ 已确认：无伤大雅 | PRD 中写的是 `prototype-v21.html`，实际文件名为 `prototype-v21.0.html` |

---

## 二、角色与权限

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| R1 | §4.2 vs §4.3 | ✅ 已确认："上传下载"是独立的第三级权限 | PRD 中其他矩阵表格也有上传下载的权限列，确认"上传下载"是独立权限级别。需在 §4.2 补充其可执行的操作定义 |
| R2 | §4.3 | 在线编辑——"上传下载"角色的限制不明确 | 权限矩阵中"上传下载"角色的在线编辑写的是"✅仅自己上传的文件"。但如果该用户上传的文件经审批已发布，编辑已发布文档需要走"编辑副本"流程——这时该用户有权创建编辑副本吗？ |
| R3 | §4.1 | 系统管理员是否只有一个？ | 角色定义说"系统管理员（=总经理）"且"系统预设"，但未说明能否有多个系统管理员。§6.9 系统管理页面中系统管理员行"不显示角色管理按钮，显示'系统预设'"，暗示不可新增。请确认是否单一角色 |
| R4 | §4.2 | 审批人 = 提交人自动通过——边界情况 | 会签模式下，如果审批链有 3 人（A、B、C），提交人恰好是 B，是否 B 这一环节自动通过、A 和 C 仍需手动审批？ |
| R5 | §6.3.2 | 部门管理员 vs 部门负责人的创建子组权限 | 部门管理员"仅自己是负责人的组下"可创建子组。但部门管理员创建一级子组后自动成为该组负责人——那管理员是否可以在自己创建的子组下继续无限嵌套创建子子组？ |
| R6 | §6.3.2 公司层（3） | 公司层管理员权限范围——"自建组下"含义 | 公司层管理员创建子组权限写的是"系统管理员、公司层管理员（自建组下）"。如果管理员 A 创建了组 X，A 在 X 下又创建了 X1，那么管理员 B（也是公司层管理员）能否在 X 或 X1 下创建子组？还是只能管理各自创建的组树？ |

---

## 三、文件状态与流转

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| S1 | §5.1 + §5.3 | "编辑中"状态可见性矛盾 | §5.3 状态说明表中"编辑中"的出现位置写的是"个人中心 / 共享中心"，但"共享列表可见"列为"否"。在共享中心哪里能看到"编辑中"状态？是指共享文档文件列表的某种标识（如"编辑中"图标），还是仅在文件详情页内可见？ |
| S2 | §5.1 | 编辑中状态的文档能否删除？ | 状态机只画了 `[草稿] → 删除 → 回收站` 路径。编辑中（即草稿正在编辑）状态下，用户能否删除该文档？§6.5.2 操作矩阵中"编辑中"行没有"删除"操作，但也没有明确说明不可删除 |
| S3 | §5.1 | ⏳ 确认中："已驳回"状态的流转 | 状态机画的是 `已驳回 → 重提 → 审批中`，但"重提"前用户是否需要先"编辑"修改内容？已驳回的文档在个人中心以什么状态展示？§5.3 和 §6.5.2 的状态列表中没有"已驳回"独立出现在操作矩阵中 |
| S4 | §6.3.3 上传文件 | 审批开关关闭时的状态流转 | "审批开关关闭 → 上传 → 自动转 MD → 直接发布"。这种场景下文件是否跳过"审批中"直接变为"已发布"？版本号如何标记？ |
| S5 | §6.3.4 | 草稿不进详情页 | "草稿和编辑中状态的文档直接进入编辑器，不走详情页"——但审批中的文档呢？从个人中心"我创建的"点击一个审批中的文档，进入详情页还是编辑器？ |

---

## 四、版本管理

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| V1 | §6.3.3 | ⏳ 确认中：版本号递增规则 | 文档提到"vX.Y 自动递增"，首次上传为 v1.0。递增规则是什么？每次更新上传 +1.0（v1.0 → v2.0 → v3.0）？还是存在小版本号递增场景（v1.0 → v1.1）？回滚生成的新版本号又如何编排？ |
| V2 | §6.3.3 | 更新上传的文件名规则 | "文件名以首次上传文件名为准"——如果首次上传的是 `report.docx`，更新上传时用户选择了 `report_v2.docx`，最终文件名是 `report.docx` 还是 `report_v2.docx`？转 MD 后文件名如何处理？ |
| V3 | §6.3.4 | 版本回滚的审批流程 | "回滚生成新版本，不删除中间版本"——回滚操作是否也需要走审批流？还是管理员可直接回滚发布？ |
| V4 | §6.3.6 | 快照 vs 版本——存储粒度 | 自动保存每 10 分钟产生一个快照，手动 Ctrl+S 也产生快照。如果一个文档长期编辑，可能产生大量快照。是否有快照数量上限或自动清理策略？ |

---

## 五、在线编辑与协同

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| E1 | §6.3.5 | ⏳ 讨论中：编辑器技术选型 | PRD 提到"基于 Markdown 的所见即所得在线编辑器"，支持 Slash 菜单、工具栏、LaTeX、Word→MD。是否有推荐的编辑器库？（TipTap？Milkdown？ByteMD？）这对开发排期影响很大 |
| E2 | §6.3.7 | ⏳ 讨论中：OT vs CRDT | "基于 OT/CRDT 算法保证最终一致性"——OT 和 CRDT 是两种不同的技术方案，实现复杂度和架构差异较大。需明确选用哪一种，还是由技术侧决定？ |
| E3 | §6.3.7 | 协同编辑的并发上限 | 同一文档最多支持多少人同时编辑？§7.1 性能要求只提到"50 人同时在线"，但这是系统级并发还是单文档级？ |
| E4 | §6.3.5 | 编辑副本与协同的冲突 | "一文档同时只能有一份活跃的编辑草稿"。用户 A 创建了编辑副本正在编辑，用户 B 点击"编辑"时弹窗"是否加入协同"。如果 B 选择"取消"，B 是否就完全无法编辑该文档？有没有"排队"或"通知 A 结束编辑"的机制？ |
| E5 | §6.3.5 | 编辑器内"提交发布"流程 | 从个人中心新建草稿 → 编辑 → 点击"提交发布"。此时需要"选目标组"。但如果用户不属于任何组，能否提交？草稿是否可以不归属任何组、仅作为个人文档存在？ |
| E6 | §6.3.7 | 协作者权限——草稿发布后 | "组外人员：个人中心保留对该文档的编辑权限，但不在共享文档中可见"——这个"编辑权限"是文档级权限吗？该权限是永久保留还是有失效机制？ |

---

## 六、分享与权限

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| P1 | §6.3.8 | 无权限用户通过分享链接访问的流程 | 通知消息 M14 提到"无权限用户通过链接申请阅读"。但 §6.3.8 未描述这个场景的 UI 流程——无权限用户点击分享链接后看到什么？是一个"无权限"页面 + "申请权限"按钮？还是直接跳到登录？ |
| P2 | §6.3.8 | 已有高权限不被覆盖的边界 | "如果对方已有「可编辑」权限，这里即便选择「可阅读」，权限也不被覆盖"。那反过来——对方已有"可阅读"组级权限，分享"可编辑"时，是否创建文档级权限使其升级？ |
| P3 | §6.3.3 文档级权限 | 文档级权限可否授予组外人员？ | 弹窗描述中"添加成员"打开飞书成员选择器——这是否意味着可以给完全不在该组内的人设置文档级权限？如果是，该用户能否在共享文档列表中看到这个文件？ |
| P4 | §6.3.8 | 分享链接的生命周期 | 不做清单说明"不做链接有效期"。那链接是永久有效的？如果文档被删除或移除，链接是否自动失效？ |

---

## 七、审批流程

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| A1 | §6.3.2 组设置 | 审批链配置的详细 UI 缺失 | PRD 只提到"审批模式：依次审批 / 会签审批"和"审批人默认为组负责人"，但没有详细描述审批链的配置界面——如何添加/删除/排序审批人？是否支持多级审批？每级可以设置几个审批人？ |
| A2 | §6.4.2 | 审批超时催办上限 | M6 提到"催办达上限"通知，但没有定义催办上限次数是多少。是固定值还是可配置？ |
| A3 | §6.4.2 不做清单 | 审批人离职时需"手动调整审批链" | 但 §6.3.2 按部门（7）特殊规则说"飞书人员移除（离职）→ 若是组负责人则自动转给部门负责人 A"。如果离职的人同时在审批链中，是否也自动替换？还是确实需要手动去每个组调整？ |
| A4 | §6.4.2 | ⏳ 确认中：撤回后文档状态 | "我发起的"中"撤回"按钮 → toast"已撤回"。撤回后文档回到什么状态？"编辑中"？"草稿"？撤回后用户在哪里找到该文档继续编辑？ |

---

## 八、跨组移动

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| M1 | §6.3.3 vs M12 | ✅ 已确认：即时移动 + 异步审核，两者不冲突 | 跨组移动弹窗"确认移动 → toast 已移动"是即时操作（文件先转移给目标组成员）；M12 的"同意/拒绝"按钮是发给目标组负责人的通知，负责人可异步审核。两个流程不冲突：先执行移动，目标组负责人再收到通知进行确认 |
| M2 | §4.3 | 跨组移动的权限范围 | 权限矩阵中只有系统管理员（全局）和组负责人（本组）可执行跨组移动。"本组"是指源组还是目标组？系统管理员移动时是否也需要目标组负责人同意？ |
| M3 | §6.3.3 | 跨组移动——已审批中的文件能否移动？ | 流程图提到"状态校验"，但未列出哪些状态可以移动。推测只有"已发布"状态可移动，但需明确确认 |
| M4 | §6.3.3 | 批量跨组移动的同名检测 | 流程图提到"同名检测"，批量移动时如果部分文件同名、部分不同名，是整批拒绝还是逐个处理？ |

---

## 九、回收站

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| B1 | §6.6 vs §6.5.2 | ✅ 已确认：只有一个全局回收站 | 根据 §6.5.2，删除操作仅限"草稿且是自己创建的"才能删除。个人草稿删除后直接进入全局回收站，不存在独立的"个人回收站" |
| B2 | §6.6 | ✅ 已确认：回收站只包含被删除的个人草稿 | 删除只能对草稿且是自己创建的执行，已发布文档不可删除。回收站中只有被删除的草稿 |
| B3 | §6.6 | 回收站的权限范围 | 谁能看到回收站中的哪些文件？系统管理员看到全部？组负责人只看到自己组的？普通用户只看到自己删除的？ |
| B4 | §6.6 | 恢复后的状态 | "恢复"后文件恢复为"已发布状态，重新在原组文件列表展示"。但如果被删除的是草稿，恢复后应该是什么状态？ |

---

## 十、飞书集成

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| F1 | §6.3.3 | 飞书 Bot 归档入口缺少详细流程 | PRD 提到"飞书 Bot / DocFlow 内双入口"，DocFlow 内的导入流程有描述，但 Bot 侧的交互流程缺失——用户如何通过 Bot 发起归档？粘贴链接后 Bot 回复什么？用户选择目标组的交互是怎样的？ |
| F2 | §6.3.3 | 飞书文档导入——协作者权限映射 | "飞书文档原协作者（含飞书云文档管理员）一律映射为 DocFlow「可编辑」权限"——这些协作者如果不在目标组内，是以文档级权限形式授权？还是自动添加为组成员？ |
| F3 | §6.3.2 | 飞书同步员工账号策略 | "全部建 DocFlow 账号（含未加入任何组的人）"——全公司飞书用户都会在 DocFlow 创建账号？预计用户量是多少？这对系统用户管理和性能有影响 |
| F4 | §6.8.3 | 飞书卡片消息回调 | "用户在飞书内点击按钮后回调 DocFlow 后端执行操作"——M10（归属人转移）和 M12（跨组移动）支持在飞书内直接同意/拒绝。如果用户同时在飞书和 DocFlow 站内都点了，如何防止重复操作？ |

---

## 十一、个人中心

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| C1 | §6.5.2 | "全部"TAB 的数据去重 | "全部"= 我创建的 + 分享给我的 + 个人收藏的并集。如果我创建了一个文档并且收藏了它，在"全部"TAB 中是否只出现一次？来源标签显示哪个？ |
| C2 | §6.5.2 | 分享给我的——可编辑用户提交发布 | 操作矩阵中"编辑中 + 分享给我的（可编辑）"有"提交发布"操作。协作者提交发布时，审批流走哪个组的配置？文档归属人是谁？发布后文档归属组是原创建者选定的组吗？ |
| C3 | §6.5.2 | 提交发布弹窗——"选目标组"的范围 | 首次发布需"选目标组"。用户可选的组范围是什么？所有自己有"可编辑"及以上权限的组？还是所有自己所在的组？ |
| C4 | §6.5.3 | 离职交接——交接后的文档归属 | 离职人员的文档自动交接给部门负责人后，文档的"归属人"是否变更为部门负责人？ |

---

## 十二、通知与消息

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| N1 | §6.8.2 | 飞书消息 vs 站内消息——是否同步发送？ | PRD 列出了 M1-M25 消息，明确了站内消息的分类。但每条消息是同时发送飞书消息 + 站内消息，还是部分只走飞书、部分只走站内？ |
| N2 | §6.8.4 | 权限/分享类归入"系统通知"TAB | M14-M17（权限/分享类）归入系统通知 TAB，但从用户视角来看权限申请更偏向"待处理"类型（M14/M15 有同意/拒绝按钮）。是否考虑单独分类或归入审批通知？ |
| N3 | §6.8.2 | M25 引用失效通知——缺少站内消息分类 | M25 是 v2.1 新增的飞书通知，但 §6.8.4 的站内消息分类中未提到它归入哪个 TAB |

---

## 十三、文档引用（v2.1 新增）

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| Q1 | §6.10.5 | 引用文档"只读" vs 文档级权限优先 | §6.10 核心概念说"引用组统一只读"，但 §6.10.5 说"文档级权限（分享/协作）优先于引用组默认权限"。这意味着在引用组里点击一个文档进入详情页，如果该用户有源文档的编辑权限，实际上可以编辑。这是否会造成用户困惑（引用列表显示"只读"但点进去能编辑）？ |
| Q2 | §6.10.2 | 引用文档不参与批量操作 | 复选框为 disabled——如果列表中混合了普通文档和引用文档，全选时引用文档不被选中。需确认：翻页器底部提示"含 N 条引用"是否也意味着文件总数统计要区分？ |
| Q3 | §6.10.10 | 引用 = 指针但如何实现"实时展示" | "始终实时展示源文档的最新已发布版本"——技术上需要确认：是在目标组列表加载时实时查询源文档数据，还是通过事件驱动同步？前者简单但有延迟，后者复杂但实时 |
| Q4 | §6.10.3 | 添加引用——组筛选"所有可见组" | "当前管理员有权限查看的所有组"——管理员的"可见组"由什么决定？是只要是某个组的成员就可见？还是取决于管理层级（如部门管理员只能看到本部门的组）？ |

---

## 十四、非功能需求

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| NF1 | §7.2 | ✅ 已确认：保留当前双令牌方案 | PRD 写"JWT Token 过期时间可配置（默认 8 小时）"，当前系统实现为双令牌方案：accessToken 15m + refreshToken 7d。确认先保留当前实现，后续如需调整再修改 |
| NF2 | §7.1 | 并发用户 50 人是否足够？ | 全公司飞书用户都建账号（§6.3.2），如果公司规模较大，50 人并发是否偏低？ |
| NF3 | §7.1 | 协同编辑的性能指标缺失 | 性能要求未提及协同编辑的延迟指标（§6.3.7 提到 < 200ms），也未提及单文档最大协作人数、WebSocket 连接数上限等 |

---

## 十五、操作日志

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| L1 | §6.7.2 | 自动保存是否记录日志？ | 日志类型"文件编辑"涵盖"在线编辑保存（自动保存/Ctrl+S/手动快照）"。自动保存每 10 分钟触发一次，如果记录到操作日志中，长时间编辑会产生大量低价值日志条目。是否需要合并或过滤自动保存日志？ |
| L2 | §6.7 | 日志数据范围——按角色可见 | §4.3 中只有管理员级角色可查看操作日志，普通成员不可见。但 PRD 未定义不同管理员看到的日志范围差异——组负责人是否只看到本组操作？部门管理员看到本部门所有组？ |

---

## 十六、其他

| # | 位置 | 问题 | 详情 |
|---|------|------|------|
| O1 | §6.3.2 | 全局搜索范围 | 搜索"文档名称和组名称"，搜索范围是全部共享文档还是包含个人中心的草稿？ |
| O2 | §6.3.4 | 文件详情页 Presence 头像 | "显示当前查看该文档的人员头像"——这需要在文件详情页（只读浏览页面）也建立 WebSocket 连接来追踪在线状态。是否必需？还是只在编辑器中实现 Presence？ |
| O3 | §6.3.3 | 支持转 MD 的文件格式清单 | "所有非 MD 格式自动转为 Markdown"但"仅支持可转为 md 格式的文件"——具体支持哪些格式？docx、pdf、xlsx、pptx、txt、html？需要明确列出，否则上传校验规则无法实现 |
| O4 | §6.3.9 | 批注 vs 评论的关系 | 文件详情页底部有"评论"TAB，编辑器/详情页有"选字批注"。两者是否完全独立的数据模型？在 UI 上它们分别出现在不同位置（评论在底部，批注在右侧面板），但通知消息 M 系列中是否有区分？ |
| O5 | §6.3.3 | 飞书文档导入——评论导入 | "转 MD 后保留正文+评论"——飞书评论导入后放在文件详情页底部的"飞书评论"TAB 中，但这些评论是否也支持回复？还是只读导入？ |

---

## 优先级建议

以下问题建议**优先澄清**，因为它们直接影响数据库设计和核心流程开发：

1. ✅ **R1** — "上传下载"是独立的第三级权限（已确认，可进入 RBAC 模型设计）
2. ✅ **M1** — 跨组移动即时执行 + 目标组负责人异步审核（已确认，两者不冲突）
3. ✅ **B1/B2** — 只有一个全局回收站，仅存放被删除的个人草稿（已确认，可进入数据库设计）
4. ⏳ **V1** — 版本号递增规则（确认中）
5. ⏳ **E1/E2** — 编辑器和协同技术选型（讨论中，可先开发其他模块）
6. ⏳ **S3/A4** — 已驳回/撤回后的状态流转（确认中）
7. ✅ **NF1** — 保留当前双令牌 JWT 方案，后续按需调整（已确认）
